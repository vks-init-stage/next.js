---
framework: jest
preRun:
  command:
    - yarn
postMerge:
  env:
    TRACE_PLAYWRIGHT: true
    HEADLESS: true
  pattern:
    - 'test/unit/**/*.test.{js,ts,tsx}'
preMerge:
  env:
    TRACE_PLAYWRIGHT: true
    HEADLESS: true
  pattern:
    - 'test/unit/**/*.test.{js,ts,tsx}'
postRun:
  command:
    - node --version
version: 2.1
